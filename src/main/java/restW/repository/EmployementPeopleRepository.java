package restW.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import restW.model.EmployementPeople;

public interface EmployementPeopleRepository extends JpaRepository<EmployementPeople, Integer> {
}
